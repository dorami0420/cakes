<h1>商品一覧</h1>


<table>  
  <tr>
    <th>商品ID</th>
    <th>商品名</th>
    <th>税抜価格</th>
  </tr>  

<% @items.each do |item| %>

<div>
  <td><%= item.id %></td>
  <td><%= link_to admin_item_path(item.id) do %>
  <%= item.name %></td>
  <% end %>
  <td><%= item.price %></td>
</div>
<% end %>

</table>