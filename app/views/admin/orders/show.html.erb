<h1>注文履歴詳細</h1>

<table>

        <p>注文情報</p>
        <%= @order.created_at %><br>
        <%= @order.shipping_address + @order.name %><br>

        <%= @order.payment_method %><br>
        </tr>

        <td></td>

<p>注文内容</p>
  <% @order_d.each do |order| %>
      <tr>
         <td><%= order.item.with_tax_price%></td>
          <td><%= order.subtotal%></td>
          <% @sum += order.subtotal %>
      </tr>

        <% end %>

<tr>
</table>